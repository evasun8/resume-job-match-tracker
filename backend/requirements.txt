fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
openai==1.51.0
httpx==0.27.2
pydantic==2.9.2
pdfplumber==0.11.4
python-docx==1.1.2
playwright==1.47.0
# After `pip install -r requirements.txt`, run `playwright install chromium`
# once to download the browser binary (not installable via pip).
bcrypt==4.2.0
pyjwt==2.9.0
email-validator==2.2.0
cryptography==49.0.0
# ^ was already installed transitively (via pdfplumber -> pdfminer.six); now
# pinned as a direct dependency since BE-12 uses its Fernet symmetric
# encryption directly to encrypt/decrypt each user's stored OpenAI API key.
langsmith==0.1.147

# --- Test-only (BE-13) ---
pytest==8.3.3
